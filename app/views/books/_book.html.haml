.book-short
  .stars
    .count= "(#{book.reviews.count})"
    = '&#9733;' * book.stars + ((book.stars == book.stars.to_i) ? '' : '&#189;')

  .title= link_to book.title, resource(book)
  .authors
    by
    = partial 'authors/list', :authors => book.authors